import { MenuItems } from './menuItems.model';
import { SubMenus } from './submenus.model';

export class MenuItemsContainer {
    constructor(
        
        public menuItems?: MenuItems[]
        
    ) {
    }
}
